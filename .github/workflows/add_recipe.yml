name: Add recipe PR from issue
on:
  issues:
    types: [labeled]

jobs:
  create-pr:
    if: github.event.issue.label.name == 'recipe'
    runs-on: ubuntu-latest
    steps:
      - name: Create PR 🍕
        run: |
          echo "Issue title: ${{ github.event.issue.title }}"
          echo "Issue title: ${{ github.event.issue.title }}"